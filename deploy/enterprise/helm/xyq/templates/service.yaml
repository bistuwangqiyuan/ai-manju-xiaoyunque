apiVersion: v1
kind: Service
metadata:
  name: {{ include "xyq.fullname" . }}
  labels:
    app: {{ include "xyq.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "xyq.name" . }}
    component: api
