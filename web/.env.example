# 后端 API 地址（Railway 部署后填入，例如 https://your-backend.up.railway.app）
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Stripe 公钥（前端用，可选）
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# 站点 URL，用于回调等
NEXT_PUBLIC_SITE_URL=http://localhost:3000
